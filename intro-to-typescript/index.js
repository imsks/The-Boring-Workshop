"use strict";
function addToNumbers(a, b) {
    return a + b;
}
const varA = 2;
const varB = 5;
const result = addToNumbers(varA, varB);
function storeIntoDB(value) {
    // Store Logic
}
storeIntoDB(result);
